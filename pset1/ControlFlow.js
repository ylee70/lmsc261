let numOfInstr = prompt("how many instruments do you play?")
let isMusician = numOfInstr > 0
let is MultiInstrumentalist = numOfInstr > 1 && numOfInstr >= 4
let isABand = numOFInstr > 5;

if(isABand){
    print("Play us a song, you're the band!");

}
else is (isMultiInstrumentalist){
    print("join my band!");
    
}
